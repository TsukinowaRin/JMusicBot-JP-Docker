# docs 方針

- Any behavior change should update the relevant docs in the same task.
- Keep `docs/INDEX.md` aligned with the actual contents of `docs/`.
- Use `docs/WORKLOG.md` to preserve restartability at checkpoints.
- For complex work, update `docs/EXECPLAN_*.md` when discoveries change the approach.
